VITE_API_BASE_URL=http://localhost:4000/api/v1
# Toggle the frontend mock layer off once a real backend exists.
# VITE_USE_MOCK_API=false

# Real cookie-session login through the backend (src/Helpers/
# backendAuthClient.js) instead of the mock login — separate from
# VITE_USE_MOCK_API above. Needs the api running (docs/VPS_DEV_SETUP.md);
# also turns off the dev auto-login.
# VITE_BACKEND_API_BASE_URL=http://localhost:4000/api/v1
# VITE_BACKEND_AUTH_MODE=true

DATABASE_URL="postgresql://caspira:caspira_dev_password@localhost:5434/caspira_crm?schema=public"