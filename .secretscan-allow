# Reviewed secret-scan exceptions (Backend Phase 13). Each entry needs a reason.
# Firebase web SDK config: the web apiKey is a public identifier by design (access is
# controlled by Firebase security rules and API-key restrictions), reviewed 2026-09-25.
public/firebase-messaging-sw.js:5
src/services/firebase/firebase.js:7
# Test fixtures: random-looking strings that exercise the configuration and
# secret checks (the same files are reviewed exceptions in .gitleaks.toml).
server/src/config/config.test.js
server/src/platform/infrastructure.test.js
